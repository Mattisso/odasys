import { NstbalanceFilterPipe } from './nstbalance-filter.pipe';

describe('NstbalanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NstbalanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
