import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'nttcomptebalanceFilter'
})
export class NttcomptebalanceFilterPipe implements PipeTransform {

  transform(value: any, args?: any): any {
    return null;
  }

}
