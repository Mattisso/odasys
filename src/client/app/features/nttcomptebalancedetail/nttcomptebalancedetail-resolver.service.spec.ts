import { TestBed } from '@angular/core/testing';

import { NttcomptebalancedetailResolverService } from './nttcomptebalancedetail-resolver.service';

describe('NttcomptebalancedetailResolverService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: NttcomptebalancedetailResolverService = TestBed.get(NttcomptebalancedetailResolverService);
    expect(service).toBeTruthy();
  });
});
