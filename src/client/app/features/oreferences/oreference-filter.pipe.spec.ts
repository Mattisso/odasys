import { OreferenceFilterPipe } from './oreference-filter.pipe';

describe('OreferenceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OreferenceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
