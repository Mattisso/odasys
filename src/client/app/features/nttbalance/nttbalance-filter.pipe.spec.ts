import { NttbalanceFilterPipe } from './nttbalance-filter.pipe';

describe('NttbalanceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NttbalanceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
