import { NstbalanceinputRoutingModule } from './nstbalanceinput-routing.module';

describe('NstbalanceinputRoutingModule', () => {
  let nstbalanceinputRoutingModule: NstbalanceinputRoutingModule;

  beforeEach(() => {
    nstbalanceinputRoutingModule = new NstbalanceinputRoutingModule();
  });

  it('should create an instance', () => {
    expect(nstbalanceinputRoutingModule).toBeTruthy();
  });
});
